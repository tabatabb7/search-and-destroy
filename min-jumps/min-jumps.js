"use strict";

// Complete this algo
//start at index[0]
//read the number on [0]
//looping to number 3
//turn those values into array
//sort values min to max
//take largest number
//last number of array is new jump
// if >= i + n
//if looping through new index that we land on plus steps 
//if new index is after array, then we're done 

const minJumps = (arr) => {
  let current = arr[0];
  for(let i = arr.indexOf(current))
};
const calculateJumps = () => {};
const sortNumbers = (array) => {
  return Math.max(...array)
};

module.exports = minJumps;

let current = arr[0]
for(let i = current; i < arr.length; i++){
    if(arr[i+current])
}